name: Semgrep

on:

  push:

jobs:

  semgrep:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v7

      - uses: returntocorp/semgrep-action@v1

        with:

          config: auto
